import React from 'react';

import { TitleMobile } from '../molecules/';

const Header = () => {
  return (
    <header>
      <TitleMobile />
    </header>
  );
};

export default Header;
