import React from 'react';
import Hero from '../components/Hero';

const NavBar = () => {
    return(
        <nav>
            <section>
                <Hero />
            </section>
            <div>
                {/* < /> */}
            </div>
        </nav>
    );
}

export default NavBar;